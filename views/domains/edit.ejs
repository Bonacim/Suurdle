<%- include("../partials/header") %>

<div class="row">
    <div class="col-md-12">
        <h1 class="text-center">Edit <%= domain.name %></h1>
        <div>
            <form action="../<%= domain.slug %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input id="name" class="form-control" type="text" name="domain[name]" placeholder="Domain's name" value="<%= domain.name %>" required>
                </div>
                <div class="form-group">
                    <label for="referTo">Reference</label>
                    <input id="referTo" class="form-control" type="text" name="domain[referTo]" placeholder="Domain's reference">
                </div>
                <div class="form-group">
                    <label for="verified">Verified</label>
                    <input id="verified" class="form-control" type="checkbox" name="domain[verified]" <%=domain.verified ? "checked" : ""%>>
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit</button>
                </div>
            </form>
            <a href="./">Go back</a>
        </div>
    </div>
</div>

<%- include("../partials/scripts") %>
<%- include("../partials/footer") %>

